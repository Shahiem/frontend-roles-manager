# ===================================
#  Column Definitions
# ===================================
columns:
    name:
        label: shahiemseymor.roles::lang.field.name
        searchable: true
    description:
        label: shahiemseymor.roles::lang.field.description
        searchable: true

    permissions:
        label: shahiemseymor.roles::lang.field.permissions
        relation: perms
        select: name
        searchable: true

    users:
        label: shahiemseymor.roles::lang.field.users
        searchable: false
        select: (SELECT COUNT(1) FROM shahiemseymor_assigned_roles WHERE role_id = shahiemseymor_roles.id)
